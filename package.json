{
  "name": "digitalsaudi",
  "version": "3.0.0",
  "description": "DigitalSaudi — Saudi Digital Ecosystem on Pi",
  "scripts": {
    "deploy": "npx wrangler pages deploy . --project-name digitalsaudi",
    "deploy:preview": "npx wrangler pages deploy . --project-name digitalsaudi-preview"
  }
}
